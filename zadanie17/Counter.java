
public class Counter
{
    int initialValue = 0;
    int lastValue;
    
    void Reset(){
        System.out.println(initialValue);
    };
    void Count(){
        System.out.println(
            if (lastValue > 0);
                if i < 10
        );
    }
}
